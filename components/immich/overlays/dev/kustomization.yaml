apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: immich-dev

resources:
  - ../../base
  - namespace.yaml
  - db-secret-dev.enc.yaml
  - pvc-postgresql.yaml
  - pvc-bigboi.yaml
  - ingress.yaml
  - redis.yaml
