apiVersion: v1
kind: Service
metadata:
  name: database
  namespace: immich-staging
spec:
  selector:
    app: immich-postgresql
  ports:
  - port: 5432
    targetPort: 5432
    name: postgresql
  type: ClusterIP

