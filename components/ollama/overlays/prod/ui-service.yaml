apiVersion: v1
kind: Service
metadata:
  name: ollama-ui
  namespace: ollama
spec:
  type: ClusterIP
  selector:
    app: ollama-ui
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
