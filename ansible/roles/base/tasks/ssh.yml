---
# SSH server baseline for all nodes.

- name: Ensure SSH server is installed and enabled
  ansible.builtin.apt:
    name: openssh-server
    state: present
  notify: Restart sshd

- name: Ensure SSH service is started and enabled
  ansible.builtin.service:
    name: ssh
    state: started
    enabled: true

