---
- name: Restart containerd
  ansible.builtin.service:
    name: containerd
    state: restarted

- name: Reboot for NVIDIA driver
  ansible.builtin.debug:
    msg: "NVIDIA driver installed. Reboot the host to load the driver (e.g. 'sudo reboot')."
